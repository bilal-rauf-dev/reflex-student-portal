{
  "name": "reflex-student-portal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A redesigned student portal for university academic management. CS3014 Applied HCI, FAST-NUCES.",
  "license": "MIT",
  "scripts": {
    "test": "node --test --test-reporter=spec src/lib/*.test.js",
    "test:watch": "node --test --watch src/lib/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
